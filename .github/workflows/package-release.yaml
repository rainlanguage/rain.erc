name: Package Release
on:
  push:
    branches:
      - main
jobs:
  release:
    uses: rainlanguage/rainix/.github/workflows/rainix-rs-autopublish.yaml@main
    with:
      crate: rain-erc
    secrets: inherit
